<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= project.title %> - Project Room</title>
  <link rel="stylesheet" href="/app.css">
</head>
<body>
  <div class="container">
    <!-- Project Info Card -->
    <section class="projects">
      <h2>📂 <%= project.title %></h2>
      <p><%= project.description %></p>
    </section>

    <!-- Project Chat Card -->
    <div class="chat">
      <header>
        <h2>💬 Project Chat</h2>
      </header>

      <div id="project-messages" class="messages-area">
        <% project.messages.forEach(m => { %>
          <div class="message <%= m.role %>">
            <span><%= m.text %></span>
          </div>
        <% }) %>
      </div>

      <footer>
        <form id="project-form-chat" class="chat-input" data-project-id="<%= project._id %>">
          <input type="text" id="project-input" placeholder="Type a message..." autocomplete="off">
          <button type="submit">➤</button>
        </form>
      </footer>
    </div>
  </div>

  <script src="/script.js"></script>
  <script>
  window.currentUserId = "<%= currentUserId %>";
</script>

</body>
</html>
